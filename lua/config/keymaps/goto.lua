return {
    keys = {
        h = { "^", "Goto start of the line" },
        l = { "$", "Goto end of the line" },
        b = { "%", "Goto bracket for start/end" },
    },
    opts = {
        mode = { "n", "x", "v" },
        prefix = "g",
    },
}
